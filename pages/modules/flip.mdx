# flip
The flip module is a module which flips a coin and returns `1` for heads or `0` for `tails`.

## Example

- `1`: heads
- `0`: tails

```js
// Import module
const { flip } = require("easyscriptjs");

// Generate a result
const result = flip();

// Log result to console
console.log(result);
// 0
```
