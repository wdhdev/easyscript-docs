# log
The log module lets you use a shortened version of `console.log` in Node.js. It also variables as well.

## Example

```js
// Import module
const { log } = require("easyscriptjs");

// Logs text to console
log("Hello World");
// Hello World
```
