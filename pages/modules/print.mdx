## Print
The print module allows you to use `console.log` as if it was Python. It supports variables as well.

**Example**:

```js
// Import module
const { print } = require("easyscriptjs");

// Logs the text to console
print("Hello World");
// Hello World
```
