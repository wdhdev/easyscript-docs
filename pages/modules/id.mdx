## ID
The id module generates a random id with the specified length.
  - Max length: `64`

**Example**:

```js
// Import module
const { id } = require("easyscriptjs");

// Generate an id with a length of 24
const newId = id(24);

// Logs id to console
console.log(newId);
// 4uqv9ahgcafo8gnw415surtz
```
